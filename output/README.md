# LogicMCP Server output

This is the default output root for files produced directly by MCP Tools.
Relative output paths supplied by MCP clients are resolved below this folder.

An MCP client may instead supply an absolute destination it owns. In that case
the MCP Tool produces the file at the client-owned endpoint.
