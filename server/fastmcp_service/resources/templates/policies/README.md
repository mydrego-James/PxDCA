# Policies Directory

This directory is reserved for storing business rules, company policies, and industry compliance guidelines (e.g., ISO 27001 constraints, coding guidelines, company-specific security boundaries).

When a specific profile or workflow stage requires strict adherence to external rules, place the JSON or Markdown policy documents here. MCP Prompts can then load and inject these policies into the context window as constraints.
