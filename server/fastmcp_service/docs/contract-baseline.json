{
  "captured_at": "2026-08-08",
  "endpoint": "http://127.0.0.1:8000/mcp",
  "tools": [
    {
      "name": "generate_requirements",
      "required": [],
      "parameters": ["q0", "session_id", "answer", "profile", "output_dir"]
    },
    {
      "name": "generate_architecture",
      "required": ["session_id"],
      "parameters": ["session_id", "output_dir"]
    },
    {
      "name": "run_audit",
      "required": ["session_id"],
      "parameters": ["session_id", "output_dir"]
    },
    {
      "name": "generate_skill",
      "required": [],
      "parameters": ["mode", "customization", "output_dir"]
    }
  ],
  "prompts": [],
  "resources": []
}
